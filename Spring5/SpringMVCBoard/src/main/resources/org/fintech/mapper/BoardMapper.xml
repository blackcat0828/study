<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    
<mapper namespace="org.fintech.mapper.BoardMapper">
	<!-- 
		id는 BoardMapper interface에 있는 getList() 메서드 이름 
		resultType:select Query를 수행후 리턴되는 자료 형태를 선언
	-->
	<select id="getList" resultType="org.fintech.domain.BoardVO">
		<![CDATA[
			select * from tbl_board where bno > 0	
		]]>
	</select>
	
	<insert id="insert">
		<!-- #{속성명} :  -->
		insert into tbl_board(bno, title, contents, writer) values (seq_board.nextval, #{title},#{contents},#{writer})
	</insert>	
	
	<!-- keyProperty : 기본키(Primary Key) 지정
		order="BEFORE" : sql문을 실행하기 전 <selectKey> 내용을 먼저 수행하라는 의미
		 -->
	<insert id="insertSelectKey">
		<selectKey keyProperty = "bno" order="BEFORE" resultType="long">
			select seq_board.nextval from dual
		</selectKey>
		
		insert into tbl_board(bno, title, contents, writer)
			values (#{bno},#{title},#{contents},#{writer})
	</insert>
	
	<select id="read" resultType="org.fintech.domain.BoardVO">
		select * from tbl_board where bno = #{bno}
	</select>
	
	<delete id="delete" >
		delete tbl_board where bno = #{bno}
	</delete>
	
	<update id="update">
		update tbl_board set title = #{title}, contents=#{contents},writer=#{writer}, updatedate = sysdate where bno = #{bno}
	</update>

</mapper>